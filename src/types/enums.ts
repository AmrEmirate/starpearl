// File ini akan menyimpan enum yang kita bagi antara backend dan frontend

export enum StoreStatus {
  PENDING = "PENDING",
  APPROVED = "APPROVED",
  REJECTED = "REJECTED",
  SUSPENDED = "SUSPENDED",
}